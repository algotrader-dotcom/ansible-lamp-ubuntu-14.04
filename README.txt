# Introduction
This will install LAMP (Linux, Apache2, MySQL, PHP)

# Requirments
Edit hosts with drupaltest.dev point to IP
database & documentroot in vars.yml

# Run
ansible-playbook -i hosts playbook.yml